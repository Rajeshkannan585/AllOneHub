# AllOneHub Production Roadmap

Implemented structure:
- Firebase Auth
- Firestore collections design
- Storage folders
- Netlify SPA redirects
- PWA files

Collections:
users/{uid}
posts/{postId}
comments/{commentId}
chats/{chatId}/messages/{messageId}
notifications/{notificationId}
reels/{reelId}
followers/{uid}/following/{targetUid}

Enable:
1. Authentication providers
2. Firestore database
3. Storage
4. Cloud Messaging
5. Cloud Functions